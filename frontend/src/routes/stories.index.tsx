import { createFileRoute } from "@tanstack/react-router";import { stories } from "@/lib/life-data";import { PageIntro } from "@/components/ui";import { StoryCard } from "@/components/story-card";
export const Route=createFileRoute("/stories/")({head:()=>({meta:[{title:"Stories — Life, In Receipts"},{name:"description",content:"Discovered chapters supported by connected digital moments."},{property:"og:title",content:"Discovered Stories — Life, In Receipts"},{property:"og:description",content:"Explore recurring routines and chapters found across the archive."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Stories});
function Stories(){return <div><PageIntro eyebrow="12 chapters discovered" title="Stories, found between the moments." copy="Each chapter is an observation grounded in visible dates, locations, proximity, and repetition—not an assumption about how it felt."/><div className="mt-10 grid gap-5 md:grid-cols-2">{stories.map((s,i)=><StoryCard key={s.slug} story={s} large={i===0}/>)}</div></div>}

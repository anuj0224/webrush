import { createFileRoute } from "@tanstack/react-router";import { useState } from "react";import { Minus,Plus } from "lucide-react";import { activity,months,receipts } from "@/lib/life-data";import { Button,PageIntro } from "@/components/ui";import { LifePulse,ReceiptArtifact } from "@/components/visuals";
export const Route=createFileRoute("/journey")({head:()=>({meta:[{title:"Journey — Life, In Receipts"},{name:"description",content:"Move across a year of activity signals, clusters, and connected moments."},{property:"og:title",content:"The Journey — Life, In Receipts"},{property:"og:description",content:"An expressive map of a digital life across time."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Journey});
function Journey(){const [month,setMonth]=useState(5);const [zoom,setZoom]=useState(1);return <div><PageIntro eyebrow="A year in signals" title="The journey is not a straight line." copy="Move through rises, pauses, and clusters. Select any month to reveal the moments and patterns carrying its signal." aside={<div className="flex gap-2"><Button variant="icon" onClick={()=>setZoom(Math.max(.8,zoom-.1))} aria-label="Zoom out"><Minus className="h-4 w-4"/></Button><Button variant="icon" onClick={()=>setZoom(Math.min(1.4,zoom+.1))} aria-label="Zoom in"><Plus className="h-4 w-4"/></Button></div>}/><section className="mt-10 overflow-x-auto rounded-lg border border-border bg-card/40 p-6"><div style={{width:`${zoom*100}%`,minWidth:760}}><LifePulse onSelect={setMonth}/></div></section><div className="mt-8 grid gap-8 lg:grid-cols-[.7fr_1.3fr]"><section><p className="eyebrow">Selected period</p><h2 className="display mt-3 text-6xl text-primary">{months[month]}</h2><p className="mt-4 text-sm leading-6 text-muted-foreground">{activity[month]} moments formed this period. Late-night music and short searches created the strongest cluster around three recurring places.</p><div className="mt-7 space-y-3">{["Music + Messages after 11 PM","Three returns to Juniper Coffee","Unusual activity on the second Friday"].map(x=><button className="w-full border-l border-primary/40 py-2 pl-4 text-left text-sm text-foreground hover:border-primary" key={x}>{x}</button>)}</div></section><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{receipts.slice(month*3,month*3+3).map(r=><ReceiptArtifact key={r.id} receipt={r}/>)}</section></div></div>}
